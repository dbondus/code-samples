define([
    'backbone'
], function(Backbone) {
    "use strict";

    //should provide event methods

    /**
     * Bridge to event system
     *
     * @mixin
     */
    var Events = Backbone.Events;

    return Events;
});
